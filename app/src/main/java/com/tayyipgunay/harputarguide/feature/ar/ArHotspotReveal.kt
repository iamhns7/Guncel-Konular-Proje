package com.tayyipgunay.harputarguide.feature.ar

object ArHotspotReveal {
    /** Hizalama yeterli sayıldıktan sonra noktaların belirmesi için bekleme (ms). */
    const val DELAY_MS = 700L

    /** Tam eşleşme olmasa bile kamera açıkken otomatik gösterim (ms). */
    const val ASSISTED_FALLBACK_MS = 3_000L
}
